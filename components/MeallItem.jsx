import { StyleSheet, Text, View } from 'react-native'
import React from 'react'

const MeallItem = ({title}) => {
  return (
    <View>
      <Text>{title}</Text>
    </View>
  )
}

export default MeallItem

const styles = StyleSheet.create({})